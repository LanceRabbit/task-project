<% if @user.errors.present?%>
  <ul>
    <% @user.errors.each do |error, message| %>
      <li style="color:red"><%= t("views.user.#{error}") %>: <%= message %></li>
    <% end %>
  </ul>
<% end %>

<div class="row justify-content-md-center">
  <div class="card">
    <div class="card-header  text-center">
      註冊
    </div>
    <div class="card-body">
      <%= simple_form_for @user do |f| %>
        <div class="form-group">
          <%= f.label t('views.user.name') %>
          <%= f.text_field :name, class: 'form-control', required: true  %>
        </div>

        <div class="form-group">
          <%= f.label t('views.user.email') %>
          <%= f.email_field :email, class: 'form-control', required: true, :pattern => '[^@]+@[^@]+\.[a-zA-Z]{2,6}'  %>
        </div>

        <div class="form-group">
          <%= f.label t('views.user.password') %>
          <%= f.password_field :password, class: 'form-control', required: true  %>
        </div>

        <div class="form-group">
          <%= f.label t('views.user.password_confirmation') %>
          <%= f.password_field :password_confirmation, class: 'form-control', required: true %>
        </div>

        <div class="form-group">
          <div class="float-right">
            <%= f.submit class: 'btn btn-primary' %>
            <%= link_to t('views.back'), :back, class: 'btn btn-danger' %>
          </div>
        </div>

      <% end %>
    </div>
    <div class="card-footer text-muted">
  </div>
</div>