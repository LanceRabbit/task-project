<nav class="navbar navbar-dark navbar-static-top navbar-expand-md">
    <div class="container">
        <%= link_to t('views.task_board'), root_path, class: 'navbar-brand' %>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav mr-auto">
              <li class='nav-item'><%= link_to t('views.tasks.add'), new_task_path, class:'nav-link' %></li>
            </ul>
            <ul class="navbar-nav">
              <% if current_user %>
                <li class="nav-item dropdown">
                  <%= link_to current_user.email, user_path(current_user), class: 'nav-link dropdown-toggle', data:{ toggle:'dropdown'} %>
                  <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <%= link_to t('views.users.profile'),  user_path(current_user), class: 'dropdown-item' %>
                    <div class="dropdown-divider"></div>
                    <%= link_to t('views.users.logout'), logout_path, method: :delete, class: 'dropdown-item' %>
                  </div>
                </li>
                <li class='nav-item'>
                  <%= link_to t('views.admin'), admin_root_path, class:'nav-link' %>
                </li>
              <% else %>
                <li class="nav-item">
                  <%= link_to '登入', login_path , class:'nav-link'%>
                </li>
                <li>
                  <%= link_to '註冊', signup_path, class:'nav-link' %>
                </li>
              <% end %>
            </ul>
        </div>
    </div>
</nav>
